# claude-crew

See **[AGENTS.md](AGENTS.md)**.

That file is the cross-tool convention and holds the actual rules.
This one exists because Claude Code looks for it by name.

Deliberately a pointer rather than a copy: the same rules written
into two files start disagreeing, and the disagreement stays
invisible until an agent follows the stale one.
