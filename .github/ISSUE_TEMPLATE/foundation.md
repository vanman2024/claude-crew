---
name: Foundation
about: Platform work a feature will rest on
labels: foundation
---

## What this makes possible

Foundation work is justified by what it unblocks, never on its own terms. If
nothing downstream needs it yet, it is speculative and belongs in discovery.

## What exists now

<!-- `devctl init plan` says what the profile requires and what the repo has.
     `partial` usually means a dependency was added and the work stopped. -->

## Acceptance criteria

Verifiable from outside — a command that runs, a route that answers, a check
that fails when this regresses.

- [ ]
- [ ]

## Identity

Product:
Module:
Type: Foundation
Work Key:
