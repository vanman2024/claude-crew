# claude-crew — roadmap

Eight finish lines. The dependency graph is machine truth; this is
orientation — what is established, what is being built, what comes
next, and why.

> Generated by `devctl init compile`. Regenerate rather than edit.

## NOW

**Project runnable** — Repo, local dev, database, tests, CI, a staging path
  blocked on: S43, S44

## NEXT

**Integrated and verified** — Browser E2E, full-stack contracts, staging green
  blocked on: S44

**Production ready** — Shipped, with smoke checks and a rollback path
  blocked on: S43, S45

## LATER

**Foundation operational** — Identity, tenancy, authorization, app shell

## UNSCHEDULED

**Product understood** — Intake complete, PRODUCT.md established

**Application mapped** — APP.md, DESIGN.md, journeys and workflows exist

**First complete loop** — One vertical user journey works end to end

**Features expanded** — Independent feature groups delivered

## Foundation order

Dependency order, not severity order. Each is unsafe to build
before the one above it exists.

 1. **Extend Application / API Service Layer** (`S08`)
    A real seam exists: two dot-sourced libraries (_session-config.ps1, _session-brief.ps1) are loaded first by every entry script, so config resolution a
 2. **Extend Security** (`S38`)
    Credential scanning only arrived with this initialization - the .git/hooks/pre-commit hook did not exist before and was written by devctl scaffold in 
 3. **Extend Reliability & Resilience** (`S40`)
    Genuinely strong for a project this size: restore-session.ps1 plus psmux-dispatch -Continue recover a crashed session with its prior conversation, che
 4. **Extend Observability** (`S41`)
    poll-worktrees.sh with poll-format.js and orchestrator-hook.sh give a live view of what every worker is doing, which is the highest-value observabilit
 5. **Extend Environments, CI/CD & Delivery** (`S43`)
    The highest-value gap in the repository. 126 Pester tests exist and all 126 pass, but nothing runs them: before this initialization there was no .gith
 6. **Extend Testing & Quality Architecture** (`S44`)
    Content is better than typical for a plugin repo and notably well-targeted: 126 tests across five files covering brief generation, config resolution, 
 7. **Extend Releases & Versioning** (`S45`)
    Three sources of truth for one version number and they disagree. CHANGELOG.md documents releases up to 0.4.2 dated 2026-06-23 in disciplined Keep-a-Ch
